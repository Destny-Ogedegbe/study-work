#include "main.h"

/**
 * _isupper - checks if a character is uppercase or not
 * @c: character to be tested
 * Return: 1 whether it is and 0 if not
 */

int _isupper(int c)
{
	if ((C >= 'A') && (c <= 'Z'))
	{
		return (1);
	}

	return (0);
}
